# Cleaned Data

This folder contains processed and cleaned datasets generated by the data cleaning scripts.

These files are created after:
- handling missing values
- standardizing formats
- removing duplicates

The cleaned data is used for analysis and SQL querying.

To regenerate this data:
- run the cleaning script in the `scripts/` directory